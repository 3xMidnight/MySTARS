package myBoundaries;

import myEntities.User;

/**
 * Provide a interface used to display options to respective user.
 * 
 * @author
 *
 */
public interface Menu {
	/**
	 * A static method called to display option.
	 * 
	 * @param user
	 *            user who call this method.
	 */
	public static void display(User user) {
	}
}
